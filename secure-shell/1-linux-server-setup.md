Download virtual box at
www.virtualbox.org

Download ubuntu iso at
www.ubuntu.com/server/download


After install run 

```sh
sudo apt update -y && sudo apt upgrade -y && sudo apt full-upgrade -y 
```

```sh
sudo apt autoremove
```